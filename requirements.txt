# Interview Transcriber — Python dependencies
#
# Install with: pip install -r requirements.txt
#
# Note: pyaudiowpatch must be installed separately on Windows:
#   pip install pyaudiowpatch
# It is not on PyPI under this name on all platforms.

# Audio capture
pyaudiowpatch
soxr
soundfile

# Transcription
faster-whisper

# Diarization
pyannote.audio
omegaconf

# Utilities
numpy
torch
